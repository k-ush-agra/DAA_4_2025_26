1. Take input strings s and t
2. Let n = length of s, m = length of t

3. Create a DP table dp[n+1][m+1]

4. Initialize:
   dp[i][0] = 1 (empty string t can always be formed)

5. Fill the DP table:
   If s[i-1] == t[j-1]:
      dp[i][j] = dp[i-1][j-1] + dp[i-1][j]
   Else:
      dp[i][j] = dp[i-1][j]

6. Final answer is dp[n][m]

7. Print the result